import {styled} from '@mui/material/styles';
import {Box} from '@mui/material';

const FAQ = styled((props) => {
    return (
        <Box {...props}>
            
        </Box>
    );
})``;

export default FAQ;
