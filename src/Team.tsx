import {styled} from '@mui/material/styles';
import {Box} from '@mui/material';

const Team = styled((props) => {
    return (
        <Box {...props}>
            
        </Box>
    );
})``;

export default Team;
